<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Entitlements for the generated per-app launcher .app (IOSCLaunch). It does
         almost nothing privileged: it only connects to the root ioscd daemon over a
         Unix socket in /var/jb/tmp and reads its own bundle. -->

    <!-- AppSync installs it unsigned; this lets AMFI run the non-platform binary. -->
    <key>com.apple.private.amfi.can-allow-non-platform</key>
    <true/>
    <key>get-task-allow</key>
    <true/>

    <!-- The app sandbox would otherwise block connect() to /var/jb/tmp/ioscd.sock.
         A path exception (and no-container, so /var/jb is visible) clears it. -->
    <key>com.apple.private.security.no-container</key>
    <true/>
    <key>com.apple.security.exception.files.absolute-path.read-write</key>
    <array>
        <string>/var/jb/tmp/</string>
        <string>/private/var/jb/tmp/</string>
    </array>
</dict>
</plist>
