#!/usr/bin/env bash
set -euo pipefail

: "${IOS_CLANG:?IOS_CLANG is required}"
: "${IOS_SDKROOT:?IOS_SDKROOT is required}"

exec "$IOS_CLANG" \
  -arch arm64 \
  -isysroot "$IOS_SDKROOT" \
  -miphoneos-version-min="${IOS_MIN:-16.0}" \
  "$@"
