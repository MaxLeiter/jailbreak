#!/bin/sh
set -e

prefix="@MEMO_PREFIX@@MEMO_SUB_PREFIX@"
if [ -x "$prefix/bin/update-mime-database" ] && [ -d "$prefix/share/mime" ]; then
    "$prefix/bin/update-mime-database" "$prefix/share/mime" >/dev/null 2>&1 || true
fi

exit 0
