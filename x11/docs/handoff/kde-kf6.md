# kde-kf6 — Qt6 modules (DONE) + KF6 + KWin W1 (DONE)

## Ownership
The KDE Plasma flavor: the Qt6 stack + the KDE Frameworks 6 layer, cross-built Linux→iOS in Docker/Procursus. Background priority (iosc + GNOME are the active demos), but a long-running build track.

## Qt6 modules — DONE
- Phase 2 COMPLETE: all 6 modules packaged (11 debs in `x11/linux-build/out/`, ~14:23 timestamps): qt6-shadertools, qt6-declarative (+ QtQuick + Controls iOS style), qt6-5compat, qt6-wayland (libQt6WaylandClient + the wayland QPA plugin `libqwayland-generic.dylib` — puts Qt windows on iosc), qt6-svg, qt6-image-formats — each with -dev pairs. Built vs the round-2 qtbase 6.6.3-2.
- All staged into build_base on `procursus-vol-qt` (the KF6 wave-0 ancestor). Recipe: `x11/linux-build/recipes/qtbase.mk` + the module recipes; build via `build-qt.sh` (NOT raw make). 5 one-time recipe walls fixed + committed (1bc1b6a, cef1068, 9197a3d, f5cf809, 49a34e6 + OOM guard 85aa99a). Memory: x11-qt-modules-build.
- Remaining (not started): on-device Q2 validation (qml -platform offscreen → wayland w/ QT_QUICK_BACKEND=software on iosc → a QtWidgets app); control-Depends audit + minos stamping before any repo publish.

## KF6 / KDE Plasma — DONE
- `x11/linux-build/build-kf6.sh` — 6-wave driver: stage-1 native host tooling (ECM/kcoreaddons/karchive/kconfig/ki18n/kpackage) then the 40-unit cross build (kwin + plasma-nano/mobile closure, an audited subset — NOT all ~80). Recipes code-gen'd from `tools/gen-kf6-recipes.py` TABLE (edit the generator, not the .mk). Plan/audit: `x11/docs/kde-plasma-plan.md`. Memory: x11-kf6-layer.
- Built on `procursus-vol-kf6` (clone of the Qt volume) with `--cpus=8`; latest log is `x11/linux-build/kf6-build.log` and exits 0. The successful final targeted run was `TARGETS="kglobalacceld" bash /work/build-kf6.sh`; it also copied the built KF6/Plasma debs into `x11/linux-build/out/`.
- Output check: 77 KDE/KF6-related debs in `x11/linux-build/out/` across the 40 units (runtime/dev splits plus data-only packages). Key milestones present: `kf6-kirigami`, `kf6-kio`, `kf6-kcmutils`, `kwayland`, `kdecoration`, `layer-shell-qt`, `plasma-activities`, `plasma-wayland-protocols`, `kglobalacceld`.
- Forward-scan fixes folded back into the generator: KIO drops unconditional Qt6Test, treats the Darwin `getxattr()` branch as iOS too, and avoids DiskArbitration; KCMUtils drops unconditional Qt6Test; KGlobalAccelD builds with `-DWITH_X11=OFF` so it skips private `qtx11extras`/`KStartupInfo` paths and the XCB plugin. `kf6-common.mk` pins gettext tools to host `/usr/bin/*` so cross builds do not execute staged iOS gettext binaries.

## KWin W1 — DONE
- `x11/linux-build/build-kwin.sh` now builds and packages the first-light KWin Wayland compositor layer on `procursus-vol-kf6`. Output debs are in `x11/linux-build/out/`: `kwin_6.1.5+ios1_iphoneos-arm64.deb`, `kwin-dev_6.1.5+ios1_iphoneos-arm64.deb`, plus rebuilt shim packages `libdrm2/libdrm-dev`, `libgbm1/libgbm-dev`, and `libdisplay-info1/libdisplay-info-dev`.
- Verified package contents: runtime `kwin` deb includes the `kwin_wayland` and `kwin_wayland_wrapper` app bundles under `/var/jb/Applications/KDE/` plus `/var/jb/usr/lib/libkwin.6.1.5.dylib`. Latest SHA256 after local DER re-sign pass: `kwin_6.1.5+ios1_iphoneos-arm64.deb` = `0647b71abfc314b4f52abfcd25ae5bda388252d9becd1ab54580298d4e933904`; `kwin-dev_6.1.5+ios1_iphoneos-arm64.deb` = `1ff929cf60e12e46f2190511a2fd281e8578fccb235a12b6d504b618a5bf42ab`.
- Entitlement productization is in the recipe: `kwin.mk` signs the runtime package with `iosc-gl-ent.xml` and `nogeneral`, and `ldid -e` on packaged/installed `kwin_wayland` shows the ANGLE/IOSurface platform + IOKit user-client entitlement set. The package split also carries app wrappers under `/var/jb/Applications` so the signed compositor entrypoint is actually installable.
- First-light scope: X11, DRM, virtual, libinput, KWindowSystem, and IdleTime static plugin paths are trimmed/guarded on Apple; nested Wayland + fake input + KGlobalAccel are kept. The private KWin QPA plugin is now built and statically imported, but its QPA OpenGL context path is disabled for the first-light package because QtGui's iOS OpenGLES private headers collide with KWin's libepoxy wrappers in those translation units. QPA raster/backing-store windows remain enabled.
- Forward-scan fixes landed for Darwin runtime APIs and compositor lifetime hazards encountered so far: `_NSGetExecutablePath`, `pipe2`/`accept4`/`SOCK_CLOEXEC` fallbacks, no-op file sealing, mkstemp SHM path on Apple, DRM lease guards, and no-OpenGL QuickView/thumbnail fallbacks.

## Device test — DONE (KWin first light)
- `x11/linux-build/prep-kde-kwin-device.sh` stages the first-light runtime set from `procursus-vol-kf6`, overlays the latest local KWin/shim debs, pushes to `/var/jb/tmp/kde-kwin-prep`, installs packages, and deliberately does not start iosc/KWin/Plasma. It writes `/var/jb/tmp/kde-kwin-prep/RUN-LATER.txt` as the handoff marker.
- On-device package state: `kwin 6.1.5+ios1`, KF6 runtime packages, KGlobalAccelD, KWayland, Qt runtime modules, and marked shim packages `libdrm2/libdrm-dev 2.4.120+ios1`, `libgbm1/libgbm-dev 21.0.0+ios1`, and `libdisplay-info1/libdisplay-info-dev 0.1.1+ios1` are installed; `apt-get check` exits cleanly. `kwin` files are under `/var/jb/Applications/KDE/...` and `/var/jb/usr/lib/libkwin*`.
- The prep script guards against stale volume Qt: if a staged deb is older than the installed device version, it is moved to `skipped-newer-installed/`. This preserves the newer ANGLE-enabled `qt6-base 6.6.3-3` and `qt6-wayland 6.6.3-1` from the staging repo (dev.repo.maxleiter.com) instead of downgrading to the old KF6-volume `6.6.3-2`/`6.6.3` builds.
- Rootless app packaging gotcha fixed: KWin app wrappers must live under `/var/jb/Applications`, not package-root `/Applications` (read-only on device). `kwin.mk` now copies staged app bundles into `$(MEMO_PREFIX)/Applications`, and the prep script rootless-normalizes the already-built local deb before pushing.
- Verification after test: installed `kwin_wayland` has the `iosc-gl-ent.xml` platform/IOSurface/IOKit entitlements; `kwin_wayland --help` works on-device; under active `iosc`, KWin accepts nested clients on `kwin-ios-test`, initializes the QPainter scene, and tears down cleanly. Evidence/logs: `x11/artifacts/device-runs/kde-kwin-20260702-215420/` (`kwin-long.log`, `kwin-client.log`, standard xios status bundle).
- KWin now advertises/imports the local `iosc_iosurface` protocol. The recipe injects a first-light `IoscClientBuffer` implementation that imports client IOSurfaces via `task_for_pid`/Mach send right extraction and exposes them to KWin's QPainter scene as CPU-readable ARGB buffers. This is intentionally a visibility bridge, not the final zero-copy KWin renderer.
- On-device retest after host DER-signing `kwin_wayland`: `/var/jb/usr/bin/qt-wayland-gl-smoke` binds `iosc_iosurface`, creates 420x260 then 1360x1000 triple-buffered IOSurface windows, and KWin logs imports. The first KWin bridge copied IOSurface rows upside down; `kwin-ios-fixes.sh` now vertically flips the CPU staging copy returned to KWin's QPainter path. Evidence/logs: `x11/artifacts/device-runs/kde-kwin-iosurface-20260702-221705/` and the upright retest below.
- Important signing finding: the in-container KWin recipe entitlements are XML-visible but DER-less. `task_for_pid` failed with `kwin_iosurface: task_for_pid(...) failed: 0x5` until the installed `kwin_wayland` was re-signed on the Mac with `xsign`/`iosc-gl-ent.xml`. Running `linux-build/resign-graphics-packages.py linux-build/out ...` repacked the local `kwin_6.1.5+ios1` deb with DER signatures; use that or the publish-time finalizer before future installs.
- Frame pacing is now cleared for the first-light path. `iosc` has the coalesced repaint pulse for mapped surfaces that request `wl_surface.frame`, and the iosc launch paths (`run-iosc.sh`, `run-shell.sh`, `xios-session`, `ioscd`) default `IOSC_FRAME_PULSE=1` while keeping the env override for debugging. On-device evidence `x11/artifacts/device-runs/kde-kwin-framepulse-20260702-234309/`: KWin QPainter initialized, imported IOSurface ids 29/36/39 and 43/45/50, the nested Qt window is visible upright, and `client.log` has no `Didn't receive frame callback in time` loop after the 12s smoke.

## Plasma Desktop packaging — WIP
- Desktop/mobile audit as of 2026-07-03: `kwin`, Qt6, KF6, `kwayland`, `kdecoration`, `layer-shell-qt`, `plasma-activities`, `plasma-wayland-protocols`, KGlobalAccelD, `libplasma`, the workspace support wave, `plasma-workspace`, a first-light `plasma-desktop` desktop-surface package, `plasma-nano`, and a first-light `plasma-mobile` mobile shell package are built. `systemsettings`, `kscreen`, `kscreenlocker`, and the heavier Plasma Desktop/Mobile KCM/plugin/service wave are not packaged yet.
- `x11/linux-build/build-plasma-desktop.sh` is the Plasma shell-layer build lane. It currently defaults to `TARGETS=libplasma-package` on `procursus-vol-kf6`, reuses the existing Qt/KF6 helper recipes, and collects desktop/nano/mobile Plasma debs into `x11/linux-build/out/`.
- `libplasma` is now built and packaged from KDE Plasma 6.1.5 with the required upstream rebuild marker: `libplasma_6.1.5+ios1_iphoneos-arm64.deb` and `libplasma-dev_6.1.5+ios1_iphoneos-arm64.deb`. Latest SHA256: `libplasma` = `7b5ea6733d741b7daadf51dae39544cbfa9a8731db58e851e1b9f1b1ef407899`; `libplasma-dev` = `fbedc9640bb9f8d1a0f1c2caa5fbb35dbfb8e398a32ff746f868be8d6f8ba470`.
- `libplasma` is built with `-DWITHOUT_X11=ON`; `linux-build/recipes/libplasma-ios-fixes.sh` wraps the remaining upstream `KX11Extras`/`KWindowInfo` call sites behind X11 guards so the Wayland/QtQuick Plasma library layer can cross-build for iOS.
- Runtime contents verified: `/var/jb/usr/lib/libPlasma.6.1.5.dylib`, `/var/jb/usr/lib/libPlasmaQuick.6.1.5.dylib`, Plasma QML imports under `/var/jb/usr/lib/qt6/qml/org/kde/plasma/...`, Kirigami Plasma style bits, package-structure plugins, and the CMake/header split in `libplasma-dev`.
- KCMUtils cross-tooling fallback is now preserved for future rebuilds: the fix is in `linux-build/recipes/kcmutils.mk`, folded back into `linux-build/tools/gen-kf6-recipes.py`, and also applied idempotently by `build-plasma-desktop.sh` for existing `procursus-vol-kf6` volumes.
- Workspace support wave is now built and collected into `linux-build/out/`: `plasma-activities-stats`, `kf6-attica`, `kf6-declarative`, `kf6-runner`, `kf6-kded`, `kf6-statusnotifieritem`, `kf6-unitconversion`, `kf6-parts`, `kf6-newstuff`, `kf6-wallet`, and `kf6-notifyconfig`, all with runtime/dev pairs and `+ios1` upstream rebuild markers. Metadata verification passes in the build container via `dpkg-deb -f` for every package.
- First-light cuts for that support wave are intentional: `KWallet` is API-only (`BUILD_KWALLETD=OFF`, `BUILD_KWALLET_QUERY=OFF`), `KNotifyConfig` demotes the Phonon fallback so sound-preview support can stay off, `KStatusNotifierItem` disables X11 probing and patches out the macOS-only AppKit helper on iOS, and `KNewStuff` disables optional Kirigami2/Syndication branches.
- `plasma-workspace` first-light is now built and collected into `linux-build/out/`: `plasma-workspace_6.1.5+ios1_iphoneos-arm64.deb` SHA256 `2c8df3770b2c4c71a03fcc183f1bd76e150a62a2e6c040b47b85fe641a5c0eee` after host DER finalization, and `plasma-workspace-dev_6.1.5+ios1_iphoneos-arm64.deb` SHA256 `a9edf03414851d93965febbc3e28d938afcc2221887f42e74c0caa74b53568de`. The runtime deb includes `plasmashell.app` and `plasmawindowed.app` under `/var/jb/Applications/KDE/`, plus `libkworkspace`, `libtaskmanager`, workspace QML modules, the status-notifier watcher plugin, and Breeze look-and-feel assets.
- `plasma-workspace` first-light cuts are intentionally Wayland-only and shell-focused: `KF6Prison`, `KF6TextEditor`, Plasma5Support, KSysGuard, KScreen/KScreenLocker, ScreenSaverDBusInterface, Phonon/Canberra, UDev, ICU hard-requires, appmenu, and broad nonessential top-level subdirs are patched out or demoted. `libtaskmanager` and `plasmashell` keep the Wayland protocol paths; stray X11 includes/calls are guarded, and Linux `pipe2()` is replaced by an Apple `pipe()` plus `FD_CLOEXEC` fallback.
- Runtime package signing now follows the ANGLE/IOSurface productization rule: `plasmashell` and `plasmawindowed` are signed with `iosc-gpu-client-ent.xml` and `nogeneral`; `ldid -e` on the extracted deb shows the AGX/IOGPU/IOSurface user-client entitlement set and no generic no-container merge. The local `plasma-workspace` deb in `linux-build/out/` was also host-finalized with `resign-graphics-packages.py` so those two app binaries carry DER entitlements. Keep the publish-time finalizer in the repo path before any future install or publication.
- `plasma-desktop` first-light is now built and collected into `linux-build/out/`: `plasma-desktop_6.1.5+ios1_iphoneos-arm64.deb` SHA256 `5a7ad3b8f840c8420712ed0678b7e0cd066a77748a66be21e984a5e88e1e93af`, and `plasma-desktop-dev_6.1.5+ios1_iphoneos-arm64.deb` SHA256 `c13325cc11f9df5b138682934b1e6abf0664935b82c368368d6c090720bcae2b`. Runtime package metadata verifies as `Version: 6.1.5+ios1`, `Installed-Size: 2316`, and depends on the packaged KWin + Plasma Workspace base.
- `plasma-desktop` first-light contents verified in the deb: desktop shell package data, desktop/panel containments, default/empty/appmenu panel layout templates, kicker/kickoff/window-list/margins/minimize/show-desktop applet packages, `krunner_kwin.so`, `krunner_plasma-desktop.so`, and the show-desktop QML plugin. The folder containment package data is present, but the folder C++ QML plugin is intentionally skipped in this wave.
- `plasma-desktop` first-light cuts are intentionally conservative: KCMs, SDDM theme install, account integration, solid automounter, emojier, kaccess, kimpanel, trash plugin, taskmanager plugin, pager plugin, activity-manager import plugin, Plasma5Support, KSysGuard, LibNotificationManager, LibColorCorrect, KScreen/KScreenLocker DBus interface packages, PackageKit, Canberra, UDev, ICU hard-requires, X11/XCB helpers, and XKB configure checks are patched out or demoted. The next desktop package wave should decide whether to port the taskmanager/pager/folder plugins or add missing support packages first.
- `plasma-nano` first-light is now built and collected into `linux-build/out/`: `plasma-nano_6.1.5+ios1_iphoneos-arm64.deb` SHA256 `c6d4c26d6836be6f733e14c88b3ebd0ca8c7954cd2b13250b8bafebef4ec59c0`, and `plasma-nano-dev_6.1.5+ios1_iphoneos-arm64.deb` SHA256 `d8c3d52578e25dba4f60a11e4f5274b9f30cd62c2b8b6738acdc85bb20025cd1`. Runtime contents verified: `org.kde.plasma.nano` shell package data, desktop toolbox package data, and `org/kde/plasma/private/nanoshell/libplasmananoshellprivateplugin.dylib`; the only source trim guards the stray `KX11Extras` fullscreen overlay path and skips translations.
- `plasma-mobile` first-light is now built and collected into `linux-build/out/`: `plasma-mobile_6.1.5+ios1_iphoneos-arm64.deb` SHA256 `7a4c6e6c8f89bb6e695951d29bfc8c74f4756ab3d768a8a0ddc8e7713924e9ff`, and `plasma-mobile-dev_6.1.5+ios1_iphoneos-arm64.deb` SHA256 `57bce99a234c0b15debfbd5242be5cc715ab9d18450c035fe4ccb00ea29a7128`. Runtime metadata verifies as `Version: 6.1.5+ios1`, `Installed-Size: 1516`, with dependencies on the packaged KWin + Workspace + Nano base.
- `plasma-mobile` first-light contents verified in the deb: `/var/jb/usr/bin/startplasmamobile`, Breeze Mobile look-and-feel, mobile homescreen/panel/taskpanel package data, quicksettings package data, metainfo, and the small `org.kde.plasma.mobile.taskpanel.so` applet plugin. It is a QML-first shell package rather than the final full mobile service stack.
- `plasma-mobile` first-light cuts are intentional: `ModemManagerQt`/`NetworkManagerQt` hard-requires are removed, `KF6Screen`, `KF6KirigamiAddons`, XCB, Libudev, and the KWin DBus interface are optionalized/demoted, and the top-level build is trimmed to `bin`, `containments`, and `quicksettings`. `components`, `kcms`, `kded`, `kwin`, `envmanager`, `initialstart`, most C++ applet backends, homescreen plugin backends, and hardware/service integrations remain deferred.
- No device install/test was done for `libplasma`, `plasma-workspace`, `plasma-desktop`, `plasma-nano`, or `plasma-mobile`; the user asked to stop touching the device.
- Plasma launchers are host-prepped but not device-tested. `x11/wayland/run-kde-plasma.sh` starts iosc as the output compositor with `IOSC_FRAME_PULSE=1`, then runs `kwin_wayland --wayland-display wayland-0 --socket kwin-ios-test` and starts GPU-entitled `plasmashell` against KWin's socket inside one `dbus-run-session`. `KDE_PLASMA_FLAVOR=desktop|nano|mobile` selects the shell package; the Nano/Mobile paths set `PLASMA_DEFAULT_SHELL=org.kde.plasma.nano` or `org.kde.plasma.mobileshell` instead of using upstream `startplasmamobile`'s missing envmanager/startplasma-wayland service stack.
- `xios-session 1.0.11` now exposes `kde`/`plasma`/`kde-desktop`, `kde-nano`/`plasma-nano`, and `kde-mobile`/`plasma-mobile` presets, and the Xios in-app picker exposes separate KDE Plasma, Plasma Nano, and Plasma Mobile rows. Local package verification: `xios-session_1.0.11_iphoneos-arm64.deb` contains the updated `run-kde-plasma.sh`, `xios-session-lib.sh`, and CLI presets; SHA256 `0dc84c7527096f306c258857c119361f22459a0cacd78f7d2e2290892991187f`.
- `x11/linux-build/prep-kde-kwin-device.sh --stage-only` now prepares the full first-light KDE set without contacting the device: Qt/KF6/KWin, `qcoro6`, `libplasma`, `plasma-workspace`, `plasma-desktop`, `plasma-nano`, `plasma-mobile`, support-wave debs, and `xios-session`. It overlays local runtime debs, filters only the unfinished `xios-kde` meta package, and dedupes by Debian version so the staged set keeps `qt6-base 6.6.3-4+ios1` and `xios-session 1.0.11` instead of older local artifacts. The first device install attempt proved `qcoro6` is required by `plasma-workspace`; keep it in the staged runtime set.
- No device install/test was done for the KDE launcher; the user asked to stop touching the device.

## Known KF6 walls pre-staged
- host qtwaylandscanner via QT_HOST_PATH; kguiaddons/kidletime need `-DWaylandScanner_EXECUTABLE=/usr/bin/wayland-scanner` in KF6_CMAKE_FLAGS (same trap qt-modules hit, adopted). if(APPLE) whack-a-mole pre-staged as audited TABLE seds for 5 risky units (3ccd62a). Downward gates already closed: qtbase round-2 printsupport (kxmlgui), qt5compat (KIO).

## Next
- Plasma Desktop next gate: device-test the host-prepped `xios-session kde` launcher with the built `kwin` + `plasma-workspace` + first-light `plasma-desktop` base, then expand the desktop package toward the taskmanager/pager/folder plugins and KCM layer. Expect to keep patching or disabling nonessential desktop integrations first: systemd/logind/session management, kscreenlocker, powerdevil, geoclue, networkmanager-qt/modemmanager, PipeWire/audio helpers, UDev device notifications, and X11-only code paths.
- Plasma Mobile/Nano next gate: device-test `xios-session kde-mobile` and `xios-session kde-nano` above the built `plasma-nano`/`plasma-mobile` base. The next package wave should restore missing support/plugins behind KirigamiAddons, QCoro DBus, KF6Screen, NetworkManagerQt/ModemManagerQt, LibKWorkspace, KWin DBus interface, UDev, and iOS service bridges. Keep extending the entitlement rule from KWin: GL-initializing real processes such as `plasmashell`, `qml`, and future shell helpers need the same iosc GPU/platform entitlement treatment.
- Turn the KWin smoke command into a small reusable launcher/test helper so future Plasma work does not depend on a hand-written `dbus-run-session` block.

## Docker gotchas
- Volume-prune KILLS active builds (transient containers look unmounted between packages) — use builder-prune. VM: 16 CPU / 7.7GiB → default ninja -j18 OOMs big TUs (qmldom) → full-speed pass then -j2 retry.
