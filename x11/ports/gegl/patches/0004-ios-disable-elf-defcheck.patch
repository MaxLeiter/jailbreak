--- a/meson.build
+++ b/meson.build
@@ -660,7 +660,9 @@ subdir('po')
 subdir('docs')
 
 
-if not os_osx and host_cpu_family != 'x86'
+# defcheck.py looks for ELF .so files and invokes GNU nm options.  Darwin
+# produces Mach-O .dylibs, including for the iOS Wayland build.
+if host_machine.system() != 'darwin' and not os_osx and host_cpu_family != 'x86'
   nm = find_program('nm', required: false)
 
   # Verify .def files for Windows linking.
