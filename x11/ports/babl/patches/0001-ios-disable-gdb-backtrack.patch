--- a/babl/babl-internal.c
+++ b/babl/babl-internal.c
@@ -67,6 +67,10 @@
 int
 babl_backtrack (void)
 {
+#ifdef __ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__
+  /* iOS marks system(3) unavailable and does not provide an attachable gdb. */
+  return -1;
+#else
   char buf[512];
 
   snprintf (buf, sizeof (buf), "echo bt>/tmp/babl.gdb;"
@@ -74,6 +78,7 @@
            "gdb -q --batch -x /tmp/babl.gdb --pid=%i | grep 'in ''babl_die' -A40", getpid ());
   return system (buf);
+#endif
 }
 
 void
