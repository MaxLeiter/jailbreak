{
  "tagline": "The Ladybird browser for iPhone and iPad",
  "developer": "Ladybird Browser Initiative / Max",
  "homepage": "https://ladybird.org",
  "description": "[Ladybird](https://ladybird.org) is a new web browser with its own engine, written from scratch rather than built on WebKit or Chromium. This is Ladybird ported to jailbroken iPhones and iPads.\n\nIt installs as a normal home-screen app. Tap the icon and the browser opens.\n\nWorks on both root and rootless jailbreaks, iOS 16 or later.\n\niPad is the tested setup. The app installs and runs on iPhone too, but I have not tested it there: the toolbar is tight, and the keyboard can cover the bottom of the page. Bug reports from iPhone are welcome.",
  "changelog": [
    {
      "version": "0.1.25+ios1",
      "date": "2026-08-01",
      "notes": "- Adds scheme-neutral runtime paths and ships the same `ladybird-app` package id for both jailbreak layouts: `iphoneos-arm64` under `/var/jb/Applications` and `iphoneos-arm` under `/Applications`\n- Keeps the supported device floor explicit at 64-bit iPhone/iPad on iOS or iPadOS 16 and newer\n- Rewrites the external `libiosexec` rpath and filesystem entitlements for each target, strips foreign rpaths from bundled dylibs, and registers the exact app path with `uicache`\n- Both payloads passed host architecture, load-path, entitlement, package-layout, and dependency audits; rootful physical-device runtime validation is still pending"
    },
    {
      "version": "0.1.23+ios1",
      "date": "2026-07-28",
      "notes": "- Declares iPhone alongside iPad (`UIDeviceFamily` 1 and 2) so the app installs and shows a home-screen icon on iPhone. **Untested on iPhone**: the layout is safe-area and DPI independent, but the toolbar is tight on small screens and there is no keyboard inset yet\n- Adds the generic `CFBundleIcons` and orientation keys that were previously iPad-only\n- Stamps `MinimumOSVersion: 16.0` in the package control, so Sileo no longer offers the package to iOS 15 devices where it cannot launch\n- No engine or frontend code changes: all six binaries are byte-identical to 0.1.22+ios1"
    },
    {
      "version": "0.1.22+ios1",
      "date": "2026-07-06",
      "notes": "- Links the real ANGLE EGL/GLES runtime for iOS GPU/WebGL\n- Verifies WebGL context creation through ANGLE Metal on A10\n- Makes WebGL bring-up tracing opt-in instead of writing logs by default\n- Keeps the standalone UIKit app self-contained with bundled helpers and dylibs"
    },
    {
      "version": "0.1.11+ios1",
      "date": "2026-07-05",
      "notes": "- Present Ladybird's IOSurface-backed front buffer through Metal\n- Use Mach bootstrap IPC on iOS for bundled helpers and IOSurface handoff\n- Friendlier address bar parsing for bare HTTPS hosts\n- Keyboard input and autofocus handoff fixes\n- Real upstream Ladybird app icon\n- Bundled fonts and self-contained app resources"
    }
  ]
}
