<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>application-identifier</key>
	<string>com.max.kioskmode</string>

	<!-- Run without a sandbox data container and as a platform binary so the app
	     can (a) write the shared config plist at
	     /var/mobile/Library/Preferences/com.max.kioskmode.plist that the
	     SpringBoard tweak reads, and (b) enumerate installed apps via
	     LSApplicationWorkspace for the picker. Both processes run as `mobile`. -->
	<key>com.apple.private.security.container-required</key>
	<false/>
	<key>platform-application</key>
	<true/>
	<key>com.apple.private.security.no-container</key>
	<true/>
</dict>
</plist>
